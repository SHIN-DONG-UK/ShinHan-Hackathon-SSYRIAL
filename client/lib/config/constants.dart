import 'package:flutter/material.dart';

// 색상
const Color kScaffoldBackgroundColor = Colors.white;
const Color kCancelButtonColor = Colors.red;
const Color kNextButtonColor = Colors.blue;

// 패딩
const double kPadding = 16.0;
const double kContainerPadding = 16.0;

// 버튼
const BorderRadius kButtonBorderRadius = BorderRadius.all(Radius.circular(8));
const double kButtonFontSize = 16.0;

// 컨테이너
const BorderRadius kContainerBorderRadius = BorderRadius.all(Radius.circular(16));
Color kContainerBackgroundColor = Colors.grey[200]!;

// 텍스트
const double kTitleFontSize = 24.0;
